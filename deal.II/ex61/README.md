# H(div) method for Poisson equation

## Dirichlet problem

See `dirichlet.h` file.

```c++
cmake -DPROBLEM=1 .
make release
make
./main
```

## Neumann problem

See `neumann.h` file.

```shell
cmake -DPROBLEM=2 .
make release
make
./main
```
